@model IEnumerable<RecordFCS_Alt.Models.TipoPieza>
@using RecordFCS_Alt.Helpers

<div class="clearfix">
    <table class="table table-hover table-condensed" style="background-color:white;">


        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Status)
                    @Html.DisplayFor(modelItem => item.Orden) |
                    @Html.DisplayFor(modelItem => item.Prefijo) |
                    @Html.DisplayFor(modelItem => item.Nombre)
                    <small class="text-muted">@Html.DisplayFor(modelItem => item.Descripcion)</small>

                    <div class="pull-right">
                        @Html.NoEncodeActionLink("<span class='fa fa-plus'></span>", "Crear Tipo de Pieza para " + item.Nombre, "Crear", "TipoPieza", new { id = item.TipoPiezaID }, htmlAttributes: new { data_modal = "", data_toggle = "tooltip", @class = "btn btn-success btn-xs openModal_TipoPieza" }) |
                        @Html.NoEncodeActionLink("<span class='fa fa-gear'></span>", "Ver Atributos de " + item.Nombre, "Detalles", "TipoPieza", routeValues: new { id = item.TipoPiezaID }, htmlAttributes: new { data_toggle = "tooltip", @class = "btn btn-primary btn-xs" })
                        @Html.NoEncodeActionLink("<span class='fa fa-pencil'></span>", "Editar " + item.Nombre, "Editar", "TipoPieza", routeValues: new { id = item.TipoObraID }, htmlAttributes: new { data_modal = "", data_toggle = "tooltip", @class = "btn btn-default btn-xs openModal_TipoPieza" })
                        @Html.NoEncodeActionLink("<span class='fa fa-trash'></span>", "Eliminar " + item.Nombre, "Eliminar", "TipoPieza", routeValues: new { id = item.TipoObraID }, htmlAttributes: new { data_modal = "", data_toggle = "tooltip", @class = "btn btn-danger btn-xs openModal_TipoPieza" })
                    </div>


                    @if (item.TipoPiezasHijas.Count > 0)
                    {
                        Html.RenderPartial("_ListaHija", item.TipoPiezasHijas.ToList());
                    }
                </td>
            </tr>
        }


    </table>



</div>
